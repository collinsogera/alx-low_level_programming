#include "main.h"

/**
*print_chessboard - Function to print a chessboard
*@a: array
*
*/
void print_chessboard(char (*a)[8])
{
	int a, b;

	for (a = 0; a < 8; a++)
	{
		for (b = 0; b < 8; b++)
			_putchar(a[i][n]);
		_putchar('\n');
	}
}

